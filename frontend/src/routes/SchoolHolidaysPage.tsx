/**
 * Stub. Nav and route only - there is no school-holiday data model or API
 * yet. The term/date-range structure has to be designed first (private,
 * state and university terms differ), which is its own ticket.
 */
export function SchoolHolidaysPage() {
  return (
    <div>
      <p className="text-sm text-ink/70">
        Coming soon. School holiday terms will be recorded here so the annual planner can show them
        alongside leave.
      </p>
    </div>
  );
}
