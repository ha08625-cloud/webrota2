/**
 * Study EOI autofill: upload an NIHR Site Identification form and get it
 * back with the eleven standard sections filled in (POST /eoi/fill).
 *
 * Placeholder shell - the upload flow lands in the next task.
 */
export function EoiPage() {
  return (
    <div className="space-y-4">
      <h1 className="text-lg font-semibold">Study EOI</h1>
      <p className="text-sm text-ink/70">
        Autofill for the standard sections of a Site Identification form is coming next.
      </p>
    </div>
  );
}
