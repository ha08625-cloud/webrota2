# Plan: Archive committed rotas

# Scope

Let committed rotas be hidden from the main "Committed history" list on
`RotaPage` without affecting counters, sessions, or rollback eligibility.
Archiving is a pure visibility flag on `GeneratedRota` -- metadata only, no
engine involvement beyond one line in `rollback_commit()` (Decision 5).
Draft rotas and (already-deleted) scrapped rotas are out of scope entirely.

Out of scope, explicitly: no query parameters or server-side filtering on
`GET /rota`; no changes to counters, snapshots, sessions, or the
commit/scrap/rollback lifecycle rules; no archive action anywhere except
`RotaDetailPage`.

# Design Decisions

1. New nullable `archived_at: datetime` column on `generated_rotas`
   (migration 007). No backfill, no server_default -- exactly the migration
   006 `committed_at` pattern, because null has a defined, permanent
   meaning ("not archived"), not a temporary backfill gap.
2. Archive/unarchive apply to **committed rotas only**. Archiving a draft
   is 409. Archiving an already-archived rota is 409. Unarchiving a rota
   that is not archived is 409. Guards live in the router (mirroring
   `_require_draft`'s style), not the engine -- consistent with Decision 3.
3. Archiving has zero interaction with counters, snapshots, or sessions.
4. `rollback_commit()`'s eligibility checks ignore `archived_at` entirely:
   an archived rota stays rollback-eligible under the existing
   chain-order rule (user-confirmed). Known consequence, accepted: if the
   most recent commit is archived, the Rollback button appears only on
   that rota's detail page, reached via the Archived tab; and an
   out-of-order rollback error may name a rota that is hidden by default.
5. `rollback_commit()` **clears `archived_at`** when it flips the rota
   back to draft (user-confirmed). This is necessary, not cosmetic:
   `commit_rota()` self-heals the chain by refreshing `committed_at` to
   now on re-commit, so a surviving flag would silently re-archive a
   freshly re-committed rota with no UI action explaining it.
6. Two new endpoints, `POST /rota/{id}/archive` and
   `POST /rota/{id}/unarchive`, both returning `RotaOut` -- same shape and
   delegation pattern as `commit`.
7. `RotaSummaryOut` and `RotaOut` gain `archived_at: datetime | None`;
   the frontend `RotaSummary` and `Rota` types gain
   `archived_at: string | null` as a **required** field, matching
   `committed_at`.
8. `GET /rota` continues to return archived rotas unfiltered. The
   Committed/Archived split is purely client-side. Do not add a query
   parameter or a WHERE clause -- the frontend tab split and the
   rollback-eligibility scan (Decision 9) both depend on the full list,
   and volume is tens of rotas per year with no pagination.
9. `isMostRecentRollbackableCommit()` in `RotaDetailPage.tsx` must keep
   scanning the **full** rota list, archived rotas included. Under
   Decision 4 an archived rota can be the most recent commit; excluding
   archived rotas from the scan would make the visible most-recent commit
   wrongly show a Rollback button that 409s. Do not "tidy" this function.
10. Legacy rotas with `committed_at IS NULL` (committed before rollback
    support) are archivable -- they are committed, and since they can
    never be rolled back, archiving is the only lifecycle action left for
    them. No special-casing needed; the guard checks status, not
    `committed_at`.
11. `RotaPage` gains a "Committed" / "Archived" tab pair above the
    committed-history list, splitting on `archived_at`. The
    archive/unarchive action itself lives only on `RotaDetailPage`, next
    to the conditional Rollback button -- consistent with
    commit/scrap/rollback already being detail-page-only actions.
12. Frontend mutations follow `useRollbackCommit()`'s cache pattern
    exactly: `setQueryData` on the detail key from the response,
    `invalidateQueries` on the list key. No navigation on success.

# Task 1: Migration and model

A: No archiving capability exists today. `GeneratedRota` has `status`,
`created_at`, and `committed_at` (migration 006) only. Nothing else in
this plan has been implemented.

B: Files:
- `backend/alembic/versions/007_generated_rota_archived_at.py` (new)
- `backend/app/models/rota.py`

Deliverables:
- Migration 007: `op.add_column("generated_rotas", sa.Column("archived_at",
  sa.DateTime(timezone=True), nullable=True))` in `upgrade()`,
  `op.drop_column` in `downgrade()`. Revision `"007"`, down_revision
  `"006"`. No backfill, no server_default. Docstring stating: null means
  "not archived", permanently -- existing committed rotas read as
  unarchived, which is correct; no data migration needed; no enum
  involved.
- `GeneratedRota.archived_at: Mapped[datetime.datetime | None]` mapped
  column, `DateTime(timezone=True), nullable=True, default=None`, placed
  directly below `committed_at`. Extend the module docstring's
  migration-history note (it currently documents `committed_at` /
  migration 006) with an equivalent line for `archived_at` / 007.

C: Mirror migration 006 structurally -- read it first; it is the exact
template including the docstring style. The model change mirrors the
existing `committed_at` column definition verbatim apart from the name.
No test changes in this task; the column is exercised by Task 3's tests
and the existing Postgres migration round-trip CI job covers
upgrade/downgrade automatically.

# Task 2: Engine change (rollback_commit)

A: Task 1 is complete: the `archived_at` column exists on the model and
in migration 007. Nothing else has changed. `rollback_commit()` in
`backend/app/engine/generate.py` currently restores counters from the
snapshot, then sets `status = RotaStatus.DRAFT` and `committed_at = None`.

B: Files:
- `backend/app/engine/generate.py`

Deliverables:
- `rollback_commit()` also sets `rota.archived_at = None`, on the line
  after the existing `rota.committed_at = None`.
- Docstring updated in two places: the opening paragraph's description of
  what the flip-to-draft restores/clears, and a sentence recording *why*
  (Design Decision 5: `commit_rota()` refreshes `committed_at` on
  re-commit, so a surviving `archived_at` would silently re-archive a
  re-committed rota; "archived" only has meaning for committed rotas).

C: Do not touch the eligibility checks -- per Decision 4, none of the five
guard conditions look at `archived_at`, and the most-recent-commit query
must not filter on it. This task is the one-line assignment plus the
docstring. The behavioural test lands in Task 3 (it needs the archive
endpoint to set the flag).

# Task 3: Schemas, router endpoints, backend tests

A: Tasks 1-2 are complete: the model has `archived_at` and
`rollback_commit()` clears it. No API surface for archiving exists yet.

B: Files:
- `backend/app/api/schemas/rota.py`
- `backend/app/api/routers/rota.py`
- `backend/tests/test_api/test_rota.py`
- `backend/tests/test_api/test_rota_list.py`

Deliverables:
- `archived_at: datetime.datetime | None = None` on both `RotaSummaryOut`
  and `RotaOut`, with a short docstring line each (the classes carry
  field-by-field docstrings for `committed_at`; follow that style).
- `archived_at=rota.archived_at` added to the `RotaSummaryOut`
  construction in `list_rotas` and the `RotaOut` construction in
  `get_rota`.
- A small guard helper next to `_require_draft`, e.g.
  `_require_committed(rota)` raising 409 with a plain-string detail if
  `rota.status != RotaStatus.COMMITTED` (message style matching
  `_require_draft`).
- `POST /rota/{rota_id}/archive`: `_get_rota_or_404`, `_require_committed`,
  409 if `rota.archived_at is not None` ("already archived"), then set
  `rota.archived_at = datetime.datetime.now(datetime.timezone.utc)`,
  `db.commit()`, return `get_rota(rota_id, db=db, user=user)` -- the
  exact structure of the `commit` endpoint.
- `POST /rota/{rota_id}/unarchive`: `_get_rota_or_404`, 409 if
  `rota.archived_at is None` ("not archived"), set it to `None`,
  `db.commit()`, same `get_rota` delegation. (No `_require_committed`
  needed: a draft can never have `archived_at` set, so the
  not-archived 409 already covers it -- but note this reasoning in a
  comment so it does not look like an oversight.)
- `GET /rota` remains unfiltered (Decision 8) -- no code change, but the
  list-endpoint docstring/comment should state that archived rotas are
  deliberately included and the split is client-side.

Tests (extend the existing files, following their conventions --
`client` + `seeded` fixtures, generate-then-commit helpers already
present in the rollback test class):
- Archive happy path: generate, commit, archive -> 200, `archived_at`
  non-null in the response; `GET /rota` list row carries the same value.
- Unarchive happy path: archived rota -> unarchive -> 200,
  `archived_at` null again.
- Four 409s: archive a draft; archive an already-archived rota;
  unarchive a non-archived committed rota; unarchive a draft.
- Archive a legacy rota: commit, then null out `committed_at` directly
  via the session (the rollback tests already use this trick), archive
  -> 200 (Decision 10).
- Rollback clears the flag: generate, commit, archive, rollback-commit
  -> response has `status == "draft"` and `archived_at` null; re-commit
  -> still null (Decision 5, the re-archive-on-recommit regression).
- Rollback eligibility ignores archiving (Decision 4): commit rota A,
  commit rota B, archive B, rollback B -> 200 (B was archived but is
  still the most recent commit and rolls back fine).
- List test file: extend the existing summary-fields test to assert
  `archived_at` is present and null by default.

C: Read the `commit` and `rollback_commit_endpoint` handlers first; the
new endpoints copy `commit`'s shape (router-level guards, explicit
`db.commit()`, `get_rota` delegation), not `rollback_commit_endpoint`'s
(engine-delegated ValueError mapping) -- there is no engine function to
delegate to here, per Decision 2.

# Task 4: Frontend types, API hooks, fixtures

A: Tasks 1-3 are complete: the backend returns `archived_at` on list and
detail payloads and exposes the two POST endpoints. No frontend code
knows about archiving yet.

B: Files:
- `frontend/src/api/types.ts`
- `frontend/src/api/rota.ts`
- `frontend/src/test/fixtures/rota.ts`
- `frontend/src/api/rota.test.tsx`

Deliverables:
- `archived_at: string | null;` added as a **required** field to both
  `RotaSummary` and `Rota`, directly below `committed_at`, with a short
  doc comment in the existing style (null = not archived; only ever
  non-null on committed rotas; cleared by rollback).
- `useArchiveRota()` and `useUnarchiveRota()` in `rota.ts`, copying
  `useRollbackCommit()` exactly: `mutationFn` posting to
  `/rota/${rotaId}/archive` / `/rota/${rotaId}/unarchive` typed as
  `Rota`, `onSuccess` doing `setQueryData(rotaKeys.detail(rotaId), data)`
  then `invalidateQueries({ queryKey: rotaKeys.list() })`. Doc comments
  noting no navigation on success (Decision 12).
- `makeRotaSummary` and `makeRota` factories gain `archived_at: null` in
  their defaults. Because every fixture in the suite goes through these
  two factories with `Partial` overrides, this is the entire fixture
  blast radius -- but run the full typecheck to confirm nothing builds
  these types as raw literals elsewhere.
- Hook tests in `rota.test.tsx` for both mutations, following the
  existing `useRollbackCommit` test's pattern (MSW handler, assert
  detail cache set + list invalidated).

C: The required-field addition is deliberate (Decision 7) -- do not make
it optional to dodge fixture updates. Typecheck must pass at the end of
this task; CI's frontend gate runs typecheck before vitest.

# Task 5: RotaPage -- Committed/Archived tabs

A: Tasks 1-4 are complete: `RotaSummary.archived_at` exists and the list
payload carries it. `RotaPage` currently renders a single
"Committed history" `<ul>` from
`rotas.filter((r) => r.status === "committed")`.

B: Files:
- `frontend/src/routes/RotaPage.tsx`
- `frontend/src/routes/RotaPage.test.tsx`

Deliverables:
- A two-button tab control ("Committed" / "Archived") between the
  "Committed history" heading and the list. Local
  `useState<"committed" | "archived">` defaulting to `"committed"`.
- "Committed" tab renders `committed.filter((r) => r.archived_at === null)`
  -- today's behaviour, unchanged by default. "Archived" renders the
  inverse. Reuse the existing `<ul>` markup for both; the empty-state
  copy differs ("No committed rotas yet." / "No archived rotas.").
- No archive/unarchive action on this page (Decision 11).
- Tests: default tab shows only unarchived committed rotas and hides
  archived ones; switching tabs shows archived ones and hides the rest;
  both empty states; the draft banner is unaffected by the tab state.
  Use `makeRotaSummary({ status: "committed", committed_at: ..., archived_at: ... })`
  fixtures with a per-test MSW override of `GET /api/v1/rota`, as the
  existing tests do.

C: Style the tab buttons with the codebase's existing Tailwind vocabulary
(border/accent tokens already used on this page); no new dependency, no
Radix Tabs -- two buttons and a conditional filter are sufficient at this
scale.

# Task 6: RotaDetailPage -- Archive/Unarchive button

A: Tasks 1-5 are complete. `RotaDetailPage` currently shows, for a
committed rota, the "committed and read-only" notice plus a conditional
"Roll back commit" button gated by `isMostRecentRollbackableCommit`, and
renders commit/scrap/rollback error blocks below.

B: Files:
- `frontend/src/routes/RotaDetailPage.tsx`
- `frontend/src/routes/RotaDetailPage.test.tsx`

Deliverables:
- `useArchiveRota()` / `useUnarchiveRota()` wired in alongside the
  existing three mutations.
- In the committed branch: if `rota.archived_at === null`, an "Archive"
  button; otherwise an "Unarchive" button in its place. Both sit next to
  the conditional Rollback button, same button styling.
- `handleArchive`: `window.confirm` in the style of the existing
  handlers, copy along the lines of "Archive this rota? It will be
  hidden from the committed history list but is unaffected otherwise and
  can be unarchived at any time." `handleUnarchive`: confirm dialog is
  optional-by-symmetry -- include a simple one for consistency with every
  other lifecycle action on this page. Neither navigates on success
  (Decision 12; unlike commit/scrap, like rollback).
- Error blocks for both mutations, same pattern as the existing
  commit/scrap blocks (static message; no server detail passthrough
  needed -- the only 409s here are stale-UI races).
- `isMostRecentRollbackableCommit` is **not modified** (Decision 9). Add
  a one-line comment on the function noting that archived rotas must
  remain in the scan.
- Tests: Archive button shown for a committed non-archived rota and not
  for a draft; Unarchive shown for an archived rota; clicking each fires
  the right POST (MSW spy) after confirm; window.confirm cancellation
  aborts; error block renders on failure; Rollback button still renders
  for an archived rota that is the most recent commit (Decisions 4/9 --
  this is the regression test for the "tidying" failure mode). Follow
  the file's existing confirm-mocking and MSW conventions.

C: Read the committed-branch JSX and the three existing handlers first;
this task is additive and should not restructure them.

# Testing summary

All tests land inside Tasks 3-6 rather than as a separate pass, so each
task leaves CI green in isolation. The cross-cutting regressions to be
certain exist by the end: rollback-clears-archived-then-recommit (Task
3), rollback-of-an-archived-most-recent-commit succeeds (Task 3), and
the frontend Rollback button surviving on an archived most-recent commit
(Task 6). The Postgres migration round-trip job covers 007 with no new
CI configuration.