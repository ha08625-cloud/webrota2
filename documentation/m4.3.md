# Clinic Type PATCH + Inline Toggles (implementation plan)

## Scope

Add a partial-update path for the two clinic type booleans so the frontend can toggle them inline without round-tripping the full nested payload:

1. Backend: `ClinicTypePatch` schema + `PATCH /clinic-types/{id}`, mirroring the `DoctorPatch` pattern (all-optional fields, applied via `exclude_unset`), reusing the existing `_next_priority` / `_close_gap` helpers when `is_enabled` genuinely changes.
2. Frontend: `usePatchClinicType` hook, plus inline checkboxes for **Room required** and **Enabled** in both the Enabled and Disabled tables on `ClinicTypesPage`.

Out of scope: `name` and `category` stay PUT-only (the edit dialog remains the path for everything except the two booleans). No optimistic updates — the page keeps its existing invalidate-and-refetch pattern.

## Backend design

### Schema (`backend/app/api/schemas/clinic_type.py`)

```python
class ClinicTypePatch(BaseModel):
    is_enabled: bool | None = None
    room_required: bool | None = None
```

Export via `backend/app/api/schemas/__init__.py` like the other clinic type schemas. `clinic_priority` remains server-managed: it is absent from the schema, and Pydantic's default extra-field handling silently drops it if a client sends it — same behaviour `ClinicTypeIn` already relies on.

### Endpoint (`backend/app/api/routers/clinic_type.py`)

`PATCH /clinic-types/{clinic_type_id}`, `response_model=ClinicTypeOut`. Route ordering is not a concern here — the only static-segment collision risk on this router is `PUT /reorder`, which is a different method.

Handler logic, in order:

1. `_get_or_404`.
2. `updates = payload.model_dump(exclude_unset=True)`. An empty body is a valid no-op: skip straight to returning the row.
3. Determine whether `is_enabled` **actually changes**: `"is_enabled" in updates and updates["is_enabled"] != ct.is_enabled`. `exclude_unset` alone is not sufficient — `PATCH {"is_enabled": true}` on an already-enabled row must not touch priority, or a row that never left the partial unique index gets renumbered.
4. Capture `old_priority = ct.clinic_priority` before mutating anything.
5. Everything below inside `with _integrity_guard(db, detail):` — `_close_gap` flushes per row, so a constraint violation can surface mid-sequence, not only at commit. Detail message along the lines of `f"ClinicType {clinic_type_id} patch violates a uniqueness constraint"`.

**Enable transition (disabled -> enabled) — autoflush ordering trap.** `_next_priority` executes a SELECT, and SQLAlchemy autoflush pushes pending attribute changes before executing it. If the handler sets `ct.is_enabled = True` first and calls `_next_priority(db)` second, the autoflush persists an enabled row still carrying its stale priority and trips the partial unique index — the same failure mode the PUT handler's disabled-to-enabled comment documents. Therefore:

```python
if enabling:
    ct.clinic_priority = _next_priority(db)   # SELECT runs while row is still disabled
# only now apply the field updates
for field, value in updates.items():
    setattr(ct, field, value)
```

**Disable transition (enabled -> disabled).** Apply the field updates, then `db.flush()` (so the row is outside the index's scope), then `_close_gap(db, old_priority)` — identical order to the PUT handler.

**No transition** (same-value send, or `room_required`-only patch): apply updates, priority untouched.

The guard's `db.commit()` closes the transaction; `db.refresh(ct)` and return.

### Backend tests (`backend/tests/test_api/test_clinic_types.py`)

Additions, using the existing fixtures/factories in that file:

- PATCH `room_required` only: value flips, `is_enabled` and `clinic_priority` untouched.
- PATCH `is_enabled` false on an enabled row: row disabled, and a second enabled row above it has its priority decremented (gap closed).
- PATCH `is_enabled` true on a disabled row: row enabled with priority `max(enabled)+1`.
- Same-value PATCH (`{"is_enabled": true}` on an enabled row): 200, priority unchanged.
- Empty body `{}`: 200, nothing changed.
- Round trip disable-then-enable across three rows: final priorities are a contiguous 1..3 with the re-enabled row last.
- 404 for unknown id.
- Children untouched: PATCH on a row with schedules/eligibilities leaves child row counts and ids identical (this is the point of the endpoint — assert it).

## Frontend design

### Types (`frontend/src/api/types.ts`)

```typescript
export interface ClinicTypePatch {
  is_enabled?: boolean;
  room_required?: boolean;
}
```

### Hook (`frontend/src/api/clinicTypes.ts`)

`usePatchClinicType` (named for what it does — it takes any partial payload, not just a toggle):

```typescript
export interface PatchClinicTypePayload {
  id: number;
  payload: ClinicTypePatch;
}

export function usePatchClinicType() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ id, payload }: PatchClinicTypePayload) =>
      apiClient.patch<ClinicType>(`/clinic-types/${id}`, payload),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: clinicTypeKeys.list() });
    },
  });
}
```

Same invalidate-on-success pattern as the sibling mutations, for the same documented reason (small list, nobody mid-gesture). Confirm `apiClient` exposes a `patch` method (`frontend/src/api/client.ts`); the rota session PATCH already uses one, so it should exist — if not, adding it follows the existing method shapes.

### Page (`frontend/src/routes/ClinicTypesPage.tsx`)

Neither table currently has an Enabled column — both changes are **added columns plus a converted cell**, not a text swap:

- **Room required**: replace the static "Yes"/"No" cell with a checkbox in both tables. `onChange` fires `usePatchClinicType` with `{ room_required: e.target.checked }`.
- **Enabled**: new column in both tables, checkbox, `{ is_enabled: e.target.checked }`. It must appear in both sections to function as a control — toggling it moves the row between the two tables (after refetch), so a one-sided checkbox would be un-checkable-only.
- Keep the Enabled checkbox in its own `<td>`, not the drag handle's cell. The dnd-kit listeners are scoped to the handle button so there is no real event conflict, but a separate column avoids accidental drag-starts on touch devices.
- Give each checkbox an `aria-label` including the clinic type name (e.g. `Enabled for Duty helper`) — the page tests will select by these.

**Pending-state / reorder race.** `enabledOrder` is local state, and the reorder mutation sends the full enabled id set, which the server 409s if it does not exactly match the current enabled set. A disable-toggle in flight followed by a drag can therefore send a stale set. Mitigation, deliberately cheap:

- Disable **all** toggle checkboxes on the page while `patchClinicType.isPending` (not just the clicked one — a second toggle during flight has the same staleness problem, and per-row pending tracking is not worth it for a list this size).
- This also covers the UX gap where a checkbox would otherwise show stale state for the round-trip duration and invite double-clicks.
- The drag/toggle race is narrowed, not eliminated (a drag started before the toggle lands is still possible); the server's existing 409 + the page's existing rollback-and-error-message path is the backstop, and it already handles this correctly. Do not add locking beyond the checkbox disabling.

Surface a patch failure through a page-level error message like the existing `deleteError` / `reorderError` pattern (`toggleError` state, cleared on the next action).

### Frontend tests

`frontend/src/api/clinicTypes.test.ts`:
- `usePatchClinicType` sends PATCH to `/clinic-types/{id}` with exactly the supplied field(s) and invalidates the list on success (mirror the existing `useUpdateClinicType` test shape; add an MSW handler for the PATCH route in the test or shared handlers as the existing tests do).

`frontend/src/routes/ClinicTypesPage.test.tsx`:
- Enabled table renders an Enabled checkbox per row, checked; Disabled table renders one, unchecked.
- Unchecking Room required on a row fires PATCH with `{ room_required: false }` only.
- Unchecking Enabled on an enabled row fires PATCH with `{ is_enabled: false }` only.
- While the PATCH is in flight, all toggle checkboxes are disabled (MSW delayed response).
- A PATCH failure surfaces the error message.

## Files touched

| File | Change |
|---|---|
| backend/app/api/schemas/clinic_type.py | Add `ClinicTypePatch` |
| backend/app/api/schemas/__init__.py | Export it |
| backend/app/api/routers/clinic_type.py | Add PATCH endpoint |
| backend/tests/test_api/test_clinic_types.py | New PATCH tests |
| frontend/src/api/types.ts | Add `ClinicTypePatch` |
| frontend/src/api/clinicTypes.ts | Add `usePatchClinicType` |
| frontend/src/api/clinicTypes.test.ts | Hook test |
| frontend/src/routes/ClinicTypesPage.tsx | Checkboxes in both tables, pending/error handling |
| frontend/src/routes/ClinicTypesPage.test.tsx | Page tests |
| frontend/src/test/msw/handlers.ts | PATCH handler if the shared handlers are used (check how existing clinic type tests stub the API first) |

## Task breakdown

# Clinic Type PATCH + Inline Toggles (implementation plan)

## Scope

Add a partial-update path for the two clinic type booleans so the frontend can toggle them inline without round-tripping the full nested payload:

1. Backend: `ClinicTypePatch` schema + `PATCH /clinic-types/{id}`, mirroring the `DoctorPatch` pattern (all-optional fields, applied via `exclude_unset`), reusing the existing `_next_priority` / `_close_gap` helpers when `is_enabled` genuinely changes.
2. Frontend: `usePatchClinicType` hook, plus inline checkboxes for **Room required** and **Enabled** in both the Enabled and Disabled tables on `ClinicTypesPage`.

Out of scope: `name` and `category` stay PUT-only (the edit dialog remains the path for everything except the two booleans). No optimistic updates — the page keeps its existing invalidate-and-refetch pattern.

## Backend design

### Schema (`backend/app/api/schemas/clinic_type.py`)

```python
class ClinicTypePatch(BaseModel):
    is_enabled: bool | None = None
    room_required: bool | None = None
```

Export via `backend/app/api/schemas/__init__.py` like the other clinic type schemas. `clinic_priority` remains server-managed: it is absent from the schema, and Pydantic's default extra-field handling silently drops it if a client sends it — same behaviour `ClinicTypeIn` already relies on.

### Endpoint (`backend/app/api/routers/clinic_type.py`)

`PATCH /clinic-types/{clinic_type_id}`, `response_model=ClinicTypeOut`. Route ordering is not a concern here — the only static-segment collision risk on this router is `PUT /reorder`, which is a different method.

Handler logic, in order:

1. `_get_or_404`.
2. `updates = payload.model_dump(exclude_unset=True)`. An empty body is a valid no-op: skip straight to returning the row.
3. Determine whether `is_enabled` **actually changes**: `"is_enabled" in updates and updates["is_enabled"] != ct.is_enabled`. `exclude_unset` alone is not sufficient — `PATCH {"is_enabled": true}` on an already-enabled row must not touch priority, or a row that never left the partial unique index gets renumbered.
4. Capture `old_priority = ct.clinic_priority` before mutating anything.
5. Everything below inside `with _integrity_guard(db, detail):` — `_close_gap` flushes per row, so a constraint violation can surface mid-sequence, not only at commit. Detail message along the lines of `f"ClinicType {clinic_type_id} patch violates a uniqueness constraint"`.

**Enable transition (disabled -> enabled) — autoflush ordering trap.** `_next_priority` executes a SELECT, and SQLAlchemy autoflush pushes pending attribute changes before executing it. If the handler sets `ct.is_enabled = True` first and calls `_next_priority(db)` second, the autoflush persists an enabled row still carrying its stale priority and trips the partial unique index — the same failure mode the PUT handler's disabled-to-enabled comment documents. Therefore:

```python
if enabling:
    ct.clinic_priority = _next_priority(db)   # SELECT runs while row is still disabled
# only now apply the field updates
for field, value in updates.items():
    setattr(ct, field, value)
```

**Disable transition (enabled -> disabled).** Apply the field updates, then `db.flush()` (so the row is outside the index's scope), then `_close_gap(db, old_priority)` — identical order to the PUT handler.

**No transition** (same-value send, or `room_required`-only patch): apply updates, priority untouched.

The guard's `db.commit()` closes the transaction; `db.refresh(ct)` and return.

### Backend tests (`backend/tests/test_api/test_clinic_types.py`)

Additions, using the existing fixtures/factories in that file:

- PATCH `room_required` only: value flips, `is_enabled` and `clinic_priority` untouched.
- PATCH `is_enabled` false on an enabled row: row disabled, and a second enabled row above it has its priority decremented (gap closed).
- PATCH `is_enabled` true on a disabled row: row enabled with priority `max(enabled)+1`.
- Same-value PATCH (`{"is_enabled": true}` on an enabled row): 200, priority unchanged.
- Empty body `{}`: 200, nothing changed.
- Round trip disable-then-enable across three rows: final priorities are a contiguous 1..3 with the re-enabled row last.
- 404 for unknown id.
- Children untouched: PATCH on a row with schedules/eligibilities leaves child row counts and ids identical (this is the point of the endpoint — assert it).

### Task 1 — Backend: ClinicTypePatch schema and PATCH endpoint

**A. State of the world.** The rota generator app is feature-complete and deployed. Clinic types currently only support full-replace updates via `PUT /clinic-types/{id}` (entire nested payload, delete-and-reinsert of all child rows). This task adds a partial-update `PATCH /clinic-types/{id}` for the two booleans `is_enabled` / `room_required`, so the frontend (Task 2, later) can toggle them inline. Nothing has been completed yet on this ticket; Task 2 will add the frontend hook and page controls.

**B. Files and deliverables.**
- Read first: `backend/app/api/routers/clinic_type.py` (the `_next_priority` / `_close_gap` / `_integrity_guard` helpers and the PUT handler's transition handling), `backend/app/api/routers/doctors.py` (the `DoctorPatch` pattern to mirror), `backend/app/api/schemas/clinic_type.py`, `backend/tests/test_api/test_clinic_types.py`.
- Deliver: `ClinicTypePatch` in the schemas file (+ `__init__.py` export), the PATCH endpoint in the router, and the new tests listed in "Backend tests" above. Full backend test suite green.

**C. Instructions.** Implement exactly per the "Backend design" section: `exclude_unset`, empty-body no-op, actual-change detection for `is_enabled`, whole handler inside `_integrity_guard`. Two ordering rules are load-bearing and must not be reordered: (1) on enable, assign `_next_priority(db)` **before** setting `is_enabled = True` — the SELECT autoflushes pending changes, and an enabled row with a stale priority trips the partial unique index; (2) on disable, apply updates, `db.flush()`, then `_close_gap(db, old_priority)`. Do not touch child collections anywhere in the handler. Do not add `name`/`category` to the patch schema.

### Task 2 — Frontend: patch hook and inline toggles on ClinicTypesPage

**A. State of the world.** Task 1 (complete) added `PATCH /clinic-types/{id}` accepting `{ is_enabled?, room_required? }`, returning the full `ClinicTypeOut`, with server-side priority renumbering on genuine enable/disable transitions. This task wires it into the UI. This is the final task on the ticket.

**B. Files and deliverables.**
- Read first: `frontend/src/api/clinicTypes.ts` (existing mutation pattern), `frontend/src/routes/ClinicTypesPage.tsx` (two-table layout, `enabledOrder` local state, existing error-message pattern), `frontend/src/api/client.ts` (confirm a `patch` method exists), and the existing tests for both.
- Deliver: `ClinicTypePatch` in `types.ts`, `usePatchClinicType` in `clinicTypes.ts`, checkbox columns in both tables on the page, pending-disable and error handling, and the tests listed in "Frontend tests" above. Typecheck + vitest green.

**C. Instructions.** Implement per the "Frontend design" section. Key constraints: the Enabled checkbox must render in **both** tables (added column in each — neither table has one today); Room required converts the existing Yes/No text cell to a checkbox in both tables; keep the Enabled checkbox out of the drag-handle cell; disable all toggle checkboxes while the patch mutation is pending; keep invalidate-on-success (no optimistic updates, no cache splicing); surface failures via a page-level error message matching the existing `deleteError` pattern; `aria-label`s on every checkbox including the clinic type name. Do not modify the reorder logic — the server 409 + existing rollback already handles the residual drag/toggle race.